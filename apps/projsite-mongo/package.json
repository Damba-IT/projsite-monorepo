{
  "name": "@projsite/api-mongo",
  "version": "1.0.0",
  "description": "Projsites backend API built with Hono framework and MongoDB.",
  "scripts": {
    "dev": "bun --hot src/server.ts",
    "start": "bun src/server.ts",
    "build": "bun build src/server.ts --outdir dist --target bun --external '@projsite/types'",
    "build:prod": "bun build src/server.ts --outdir dist --target bun --minify --external '@projsite/types'",
    "clean": "rm -rf dist node_modules",
    "lint": "echo 'Add linting command here'"
  },
  "devDependencies": {
    "@types/mongodb": "^4.0.7",
    "bun-types": "latest",
    "typescript": "^5.7.3",
    "@types/node": "^22.13.1"
  },
  "dependencies": {
    "@clerk/backend": "^1.23.11",
    "@hono/clerk-auth": "^2.0.0",
    "@hono/swagger-ui": "^0.5.0",
    "@hono/zod-validator": "^0.4.2",
    "@projsite/auth": "*",
    "@projsite/types": "*",
    "hono": "^4.7.0",
    "mongodb": "^6.13.0",
    "zod": "3.24.1"
  },
  "module": "src/index.ts",
  "type": "module",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC"
}
