{
  "name": "projsite-api",
  "version": "1.0.0",
  "description": "Projsite API with both PostgreSQL and MongoDB implementations",
  "private": true,
  "scripts": {
    "dev:pg": "cd projsite-postgres && bun run dev",
    "dev:mongo": "cd projsite-mongo && bun run dev",
    "deploy:pg": "cd projsite-postgres && bun run deploy",
    "deploy:mongo": "cd projsite-mongo && bun run deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "svix": "^1.61.0"
  }
}
